import React from 'react';

const Loading = () => {
  return (
    <div className="bg-gradient-orange flex items-center justify-center min-h-screen">
      <span className="loading loading-dots w-20"></span>
    </div>
  );
};

export default Loading;